{
  "name": "sdc-praneeth-root",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "postinstall": "echo \"Skipping automatic subpackage installs. Run 'npm run bootstrap' to install Backend and frontend dependencies.\"",

    "bootstrap": "npm --prefix Backend install && npm --prefix frontend install",

    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",

    "backend": "npm --prefix Backend run start",
    "frontend": "npm --prefix frontend run dev",

    "install-all": "npm install --force && npm install concurrently pg-query-stream --save && npm --prefix frontend install --force && npm --prefix Backend install --force && npm --prefix frontend install react-router-dom @vitejs/plugin-react --force",

    "init-project": "npm run install-all && npm run dev"
  },

  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "pg-query-stream": "^4.10.3",
    "react": "^19.2.1",
    "react-dom": "^19.2.1",
    "vite": "^7.2.7"
  }
}